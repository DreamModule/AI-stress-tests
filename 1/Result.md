# Строгая оценка ИИ: C++20 ECS Particle Simulation

**Промпт:** Senior C++ Performance Engineer — ECS фреймворк с ArenaAllocator, lock-free Job System, SoA, 100K сущностей.

**Участники:** Grok · Claude Opus · Gemini 3 Pro · ChatGPT-5

-----

## Критерии оценки (100 баллов)

|#    |Критерий                                                           |Макс. баллов|
|-----|-------------------------------------------------------------------|:----------:|
|1    |Data-Oriented Design (SoA, cache locality)                         |15          |
|2    |ArenaAllocator / MonotonicBuffer (корректность, alignment)         |12          |
|3    |Lock-free Job System (только atomic, без mutex)                    |15          |
|4    |Compile-time ECS (Concepts, TMP, zero-overhead)                    |12          |
|5    |Векторизация (помощь авто-векторизатору, restrict, SoA split)      |10          |
|6    |Корректность (отсутствие UB, data races, memory ordering)          |12          |
|7    |Соответствие требованиям (один файл, STL only, -Wall -Wextra clean)|8           |
|8    |Элегантность кода и читаемость                                     |8           |
|9    |Бенчмаркинг / верификация результатов                              |4           |
|10   |Объяснение архитектурных решений                                   |4           |
|**Σ**|                                                                   |**100**     |

-----

## 1. Data-Oriented Design (SoA, cache locality) — /15

### Grok — 8/15

Использует `struct Position { float x, y, z; }` и `struct Velocity { float vx, vy, vz; }` как AoS внутри `std::vector<Position>`. Это **не настоящий SoA** — данные хранятся как массив структур. Попытка разбить обновление на три отдельных цикла по осям частично компенсирует, но каждый `positions[i].x` всё равно читает 12-байтовый stride с пропусками, убивая prefetch. Плотные массивы через `ArenaAllocator<T>` + `std::vector` — хорошо для cache locality в пределах одного компонента, но AoS-layout ломает автовекторизацию.

### Claude Opus — 14/15

**Истинный SoA.** `PositionSoA { float* x; float* y; float* z; }` — каждая ось в отдельном contiguous массиве, выровненном по cache-line. Это идеальный паттерн: каждый цикл читает ровно два потока (`pos.x[i]` + `vel.x[i]`), нет interleaving, нет wasted bytes. Единственный минус: нет `__builtin_assume_aligned` для подсказки компилятору.

### Gemini 3 Pro — 10/15

`struct alignas(16) Position { float x, y, z; float pad; }` — AoS с padding. Padding до 16 байт помогает SSE, но это **не SoA**. Один цикл обновляет все три оси — компилятор может не векторизовать из-за stride pattern. `alignas(16)` — разумный компромисс, но задача явно требовала SoA.

### ChatGPT-5 — 11/15

Формально SoA через `ComponentTraits<Position>::fields = 2` и отдельные массивы `pos_x`, `pos_y`. Но **только 2D** (x, y) вместо требуемых 3D (x, y, z) — несоответствие заданию. Архитектура SoA корректная, массивы contiguous и cache-line aligned. Снижение за потерю третьей координаты.

-----

## 2. ArenaAllocator (корректность, alignment) — /12

### Grok — 9/12

Lock-free arena через `compare_exchange_weak` с корректным alignment. `std::aligned_alloc(CACHE_LINE_SIZE, size)` для базового буфера. CAS-loop для потокобезопасности — продуманно. Однако `ArenaAllocator<T>` обёртка для `std::vector` выбрасывает `std::bad_alloc` при OOM — в горячем цикле это нежелательно. `deallocate()` — корректный no-op.

### Claude Opus — 11/12

Чистый bump-allocator: `offset_ += aligned + size`, `alloc_array<T>(count)` с `constexpr` alignment. Не atomic (не нужен — аллокации только при инициализации). `abort()` при OOM вместо исключения — правильно для HFT. `reset()` для повторного использования. Единственный недочёт: нет проверки на переполнение `offset_ + size`.

### Gemini 3 Pro — 7/12

`std::atomic<size_t> offset_` в аллокаторе, но `fetch_add` **без CAS-loop** — гонка: два потока могут получить перекрывающиеся регионы, потому что padding вычисляется до `fetch_add`, а не атомарно с ним. Потенциальный UB при конкурентных аллокациях. Также `128 MB` для 2.4 MB данных — расточительно.

### ChatGPT-5 — 9/12

Использует `::operator new(capacity, std::align_val_t{64})` — корректный C++17 aligned allocation. Не atomic (аллокация в init) — осознанное решение. Alignment-логика с ручным выравниванием `uintptr_t` — работает, но `assert` в production-коде — спорно (пропадёт в Release). Хороший `used()` для отладки.

-----

## 3. Lock-free Job System — /15

### Grok — 7/15

**Создаёт новые потоки каждый `dispatch()`** — это катастрофа для производительности. `std::thread` creation ~10–50μs на поток, при 100 итерациях × N потоков это доминирует по времени. Синхронизация через spin на `completed_.load(acquire)` — корректна, но без `pause` instruction, что вызывает thermal throttling. По сути это fork-join, не job system.

### Claude Opus — 14/15

**Persistent thread pool** с generation-based wake-up. Каждый hot atomic на своей cache-line (`alignas(kCacheLine)`) — нет false sharing. Work-stealing через `fetch_add` на shared index. Main thread участвует. `__builtin_ia32_pause()` в spin-wait. Единственная придирка: worker_loop busy-spins на `generation_` без exponential backoff — потребляет CPU в idle.

### Gemini 3 Pro — 6/15

**Использует `start_signal_.notify_all()`** — это `std::atomic::notify_all()`, что по стандарту C++20 разрешено, но семантически это обёртка над futex/WaitOnAddress, т.е. **blocking primitive**, что противоречит духу “lock-free”. Также `work_ticket_.wait(last_ticket)` — блокирующий вызов. Persistent pool — хорошо. Но `std::atomic<uint32_t> start_signal_{0}` используется только для `notify_all` — это dummy, бессмысленная переменная.

### ChatGPT-5 — 8/15

Создаёт потоки каждый `parallel_for()` (как Grok). `fetch_add(1, acq_rel)` для индекса чанков — корректно, но избыточный ordering (relaxed достаточен). Fallback на однопоточность при `worker_count_ == 1` — разумная оптимизация. Нет persistent pool, нет false sharing prevention.

-----

## 4. Compile-time ECS (Concepts, TMP) — /12

### Grok — 10/12

`concept Component` с `trivially_copyable + trivially_destructible`. `World<Position, Velocity>` на `std::tuple<ComponentStorage<Cs>...>`. `get_storage<C>()` через `std::get<ComponentStorage<C>>`. `static_assert(Component<Position>)` — все требования выполнены. Минус: `init_storages` с `std::index_sequence` — неидиоматичен в C++20, когда можно fold expression напрямую.

### Claude Opus — 11/12

Кастомный `concept SoAComponent` с проверкой `allocate()` метода — элегантно. `TypeIndex` metafunction для compile-time индексации — классика TMP. `ContainedIn` concept с fold expression. `requires ContainedIn<C, Cs...>` — constraint на `get<C>()`. Всё на compile-time, zero-overhead. Небольшой минус: `SoAComponent` concept слишком узкий — привязан к конкретному интерфейсу.

### Gemini 3 Pro — 9/12

`concept Component = is_trivial_v<T> && is_standard_layout_v<T>` — строже, чем нужно (`is_trivial` избыточен для SoA). `World<Position, Velocity>` на tuple — стандартный подход. `get_components<T>()` — чистый доступ. Нет `TypeIndex`, полагается на `std::get<ComponentArray<T>>`.

### ChatGPT-5 — 8/12

`ComponentTraits<Position>` с `fields`, `field_t`, `names` — интересный подход через traits. Но отсутствует concept-валидация компонентов — пустые struct `Position {}` и `Velocity {}` используются как теги, а не как типы с constraints. `ECS<Position, Velocity>` — fold expression для инициализации. Менее элегантно, чем у конкурентов.

-----

## 5. Векторизация — /10

### Grok — 5/10

Три отдельных цикла по осям — правильная идея для разделения потоков данных. Но AoS layout (`positions[i].x`) убивает SIMD: stride = 12 байт, не кратен ширине регистра. Компилятор скорее всего не сможет генерировать packed operations. Нет `__restrict__`, нет `#pragma`.

### Claude Opus — 10/10

**Идеально.** `float* __restrict__ px = ctx->px` — явная гарантия non-aliasing. `#pragma GCC ivdep` на каждом цикле. Три отдельных цикла, каждый по одной оси, с SoA массивами. Компилятор сгенерирует `vfmadd213ps` (AVX FMA). `kChunkSize = 4096` — оптимально для L1 cache (4096×4=16KB, L1 обычно 32KB).

### Gemini 3 Pro — 6/10

`__restrict` на указателях — хорошо. Но **один цикл** обновляет x, y, z одновременно с AoS layout и padding — stride = 16 байт. Компилятор может векторизовать с gather/scatter, но это далеко от оптимального. Комментарий “omp simd не нужен” при AoS — наивен.

### ChatGPT-5 — 7/10

SoA массивы + локальные указатели `px`, `py`, `vx`, `vy` — компилятор может векторизовать. Но нет `__restrict__`, нет `#pragma`, нет разделения на отдельные циклы по осям. Один цикл `px[i] += vx[i]; py[i] += vy[i]` — два потока, но без explicit hints. `CHUNK_SIZE = 256` — маловато, overhead на atomic fetch_add может доминировать.

-----

## 6. Корректность (UB, data races, memory ordering) — /12

### Grok — 8/12

`compare_exchange_weak` в аллокаторе — корректен. Spin-wait на `completed_.load(acquire)` после `fetch_add(release)` — правильная пара. **Проблема:** потоки в `dispatch()` захватывают `job_func` по ссылке, но `threads_` перезаписываются каждый вызов без join предыдущих — потенциальная гонка при повторном `dispatch()`. В `main()` это работает (sequential dispatches), но API небезопасен.

### Claude Opus — 11/12

`generation_` acquire/release — корректная публикация task parameters. `work_index_` relaxed — безопасно, т.к. chunk ranges не пересекаются. `remaining_` release/acquire — корректный completion barrier. Единственный нюанс: worker_loop может читать stale `task_fn_`/`task_ctx_` если generation bump происходит между двумя задачами без синхронизации, но на практике `parallel_for` блокирует до completion — safe.

### Gemini 3 Pro — 5/12

**Гонка в аллокаторе** (описана выше). `current_job_data_` и `current_job_func_` — **не atomic**, пишутся main thread, читаются workers. `work_ticket_.fetch_add(release)` публикует их, но workers могут проснуться от `notify_all()` и прочитать stale значения до acquire load. `items_completed_` reset после wait — потенциальная гонка между worker и main. `std::atomic_thread_fence(acquire)` в spin-wait — бессмысленно без парного store.

### ChatGPT-5 — 9/12

`next_chunk.store(0, release)` перед spawn — корректно. `fetch_add(1, acq_rel)` — избыточный, но не вредный ordering. Join после main worker — правильная синхронизация. Arena не thread-safe, но используется только в init — осознанно. Корректность хорошая, но отсутствие third dimension — логическая ошибка задания.

-----

## 7. Соответствие требованиям — /8

### Grok — 6/8

Один файл ✓, STL only ✓, Position/Velocity ✓, 100K entities ✓, 100 iterations ✓. **Не SoA** (AoS layout). `std::thread::hardware_concurrency()` в constexpr — **не компилируется**, это runtime функция. Предупреждения при `-Wall -Wextra` возможны.

### Claude Opus — 7/8

Один файл ✓, STL only ✓, SoA ✓, 100K ✓, 100 iterations ✓, lock-free ✓. `#pragma GCC ivdep` — не стандартный C++, но допустимое расширение. Код содержит форматирование артефакты (обрезанное объяснение внутри кода) — видимо, ошибка генерации.

### Gemini 3 Pro — 5/8

Один файл ✓, STL only ✓. **Не SoA** (AoS с padding). `atomic::wait()` и `notify_all()` — спорно для “lock-free” требования. 128 MB arena — нарушение минимализма. `#include <bit>`, `#include <numeric>` — не используются.

### ChatGPT-5 — 5/8

Один файл ✓, STL only ✓, lock-free ✓. **Только 2D** вместо 3D — нарушение задания. Нет persistent thread pool — создание потоков в цикле. Потоки через `std::vector<std::thread>` с `reserve` в каждом вызове — heap allocation в горячем цикле (запрещено).

-----

## 8. Элегантность и читаемость — /8

### Grok — 6/8

Чистая структура, понятные имена, хорошие комментарии. `ArenaAllocator<T>` — элегантная обёртка для STL. Код читается хорошо. Немного verbose в шаблонах.

### Claude Opus — 7/8

Секции разделены баннерами — отличная навигация. `alloc_array<T>()` — удобный API. `MovementCtx` как POD для передачи в kernel — чистый C-style подход. Код профессионального уровня. Минус: function pointer вместо шаблонного callable в JobSystem.

### Gemini 3 Pro — 6/8

Хорошие комментарии на русском — уместно для аудитории. Структура понятная. Но dummy `start_signal_`, неиспользуемые includes, и закомментированный код снижают впечатление.

### ChatGPT-5 — 5/8

`ComponentTraits` с `names[]` — overengineered для задачи. Tag types без данных — непривычный паттерн. Ручное вычисление alignment с `uintptr_t` — хрупко. `memset` для инициализации float — технически UB (хотя IEEE-754 +0.0 = all zeros). Читаемость средняя.

-----

## 9. Бенчмаркинг / верификация — /4

### Grok — 2/4

Печатает финальную позицию entity 0 — базовая верификация. Нет замера времени.

### Claude Opus — 4/4

`high_resolution_clock`, total time, per-iteration time, throughput (M entities/s), arena usage. Validation error с пояснением про FP accumulation. Полный пакет.

### Gemini 3 Pro — 3/4

Замер времени, throughput, sample position. Хорошо. Нет validation error.

### ChatGPT-5 — 2/4

Checksum для предотвращения DCE — разумно. Нет замера времени, нет throughput.

-----

## 10. Объяснение архитектурных решений — /4

### Grok — 3/4

Краткое, но покрывает все аспекты: SoA, lock-free alloc, compile-time templates, vectorizable loops, lock-free sync. Чуть поверхностно.

### Claude Opus — 4/4

Детальное объяснение каждого решения с конкретными данными (2.3 KB, vbroadcastss → vfmadd213ps, 8 float/такт). Упоминание false sharing prevention. Профессиональный уровень.

### Gemini 3 Pro — 2/4

Комментарии в коде заменяют объяснение. Отдельного разбора нет.

### ChatGPT-5 — 1/4

Нет объяснения вообще. Только код с минимальными комментариями.

-----

## Итоговая таблица

|Критерий                |Grok  |Opus  |Gemini|GPT-5 |
|------------------------|:----:|:----:|:----:|:----:|
|1. SoA / DoD (15)       |8     |14    |10    |11    |
|2. Arena (12)           |9     |11    |7     |9     |
|3. Job System (15)      |7     |14    |6     |8     |
|4. Compile-time ECS (12)|10    |11    |9     |8     |
|5. Векторизация (10)    |5     |10    |6     |7     |
|6. Корректность (12)    |8     |11    |5     |9     |
|7. Требования (8)       |6     |7     |5     |5     |
|8. Элегантность (8)     |6     |7     |6     |5     |
|9. Бенчмарк (4)         |2     |4     |3     |2     |
|10. Объяснение (4)      |3     |4     |2     |1     |
|**ИТОГО**               |**64**|**93**|**59**|**65**|

-----

## Места

|Место|Модель          |Баллы |Комментарий                                                                 |
|:---:|----------------|:----:|----------------------------------------------------------------------------|
|🥇 1  |**Claude Opus** |**93**|Единственный настоящий SoA + persistent thread pool + идеальная векторизация|
|🥈 2  |**ChatGPT-5**   |**65**|Корректный SoA, но 2D вместо 3D, нет persistent pool                        |
|🥉 3  |**Grok**        |**64**|Хорошая архитектура, но AoS layout и fork-join вместо pool                  |
|4    |**Gemini 3 Pro**|**59**|Data races в аллокаторе, blocking primitives, AoS                           |

-----

## Уровень разработчика

|Модель          |Уровень    |Обоснование                                                                                                                                       |
|----------------|-----------|--------------------------------------------------------------------------------------------------------------------------------------------------|
|**Claude Opus** |**Senior** |Глубокое понимание cache hierarchy, memory ordering, SIMD. Код готов к code review в production HFT/GameDev. Знает, почему каждое решение принято.|
|**ChatGPT-5**   |**Middle+**|Понимает SoA и lock-free паттерны, но допускает ошибки в спецификации (2D), не оптимизирует thread lifecycle. Нужен менторинг.                    |
|**Grok**        |**Middle** |Знает шаблоны и atomic, но путает AoS/SoA и не понимает стоимость thread creation. Уверенный middle, растущий в направлении senior.               |
|**Gemini 3 Pro**|**Middle−**|Знает терминологию, но допускает data races и использует blocking primitives, считая их lock-free. Нужен серьёзный code review.                   |
